@model IEnumerable<IngWeb.Models.fasce_terapia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ora_inizio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ora_fine)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ora_inizio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ora_fine)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.codice }) |
            @Html.ActionLink("Details", "Details", new { id=item.codice }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.codice })
        </td>
    </tr>
}

</table>
