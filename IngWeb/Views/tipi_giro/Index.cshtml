@model IEnumerable<IngWeb.Models.tipi_giro>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descrizione)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ora_inizio_fascia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ora_fine_fascia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.stato)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descrizione)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ora_inizio_fascia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ora_fine_fascia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.stato)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
